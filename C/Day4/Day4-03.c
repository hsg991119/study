#include <stdio.h>

int main()
{
	int width = 13, height = 9;
	double area = (double)(width * height) / 2;

	printf("widht = %d, ", width);
	printf("height = %d\n", height);
	printf("area = %g\n", area);

	return 0;
}